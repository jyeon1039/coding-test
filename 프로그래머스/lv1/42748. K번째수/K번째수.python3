import copy

def solution(array, commands):
    answer = []
    
    for (i, j, k) in commands:
        tmp = copy.deepcopy(array)
        tmp = tmp[i-1: j]
        tmp.sort()
        answer.append(tmp[k-1])
    
    return answer